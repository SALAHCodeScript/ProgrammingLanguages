#!/usr/bin/node

// Strings: Single Quotes
let single_quote = 'word';
// Strings: Double Quotes
let double_quote = "word";

// Integer
let x = 1;

// Float
let y = 1.0;

// Null
let nothing = null;

// Boolean: True
let isRight = true;
// Boolean: False
let isWrong = false;

// List Array: String
let str_array = ['one', 'two', 'three'];
// List Array: Integer
let int_array = [1, 2, 3];
// List Array: Float
let float_array = [1.0, 2.0, 3.0];
// List Array: Boolean
let bool_array = [true, false, false];

// Dictionary Array: String
let str_dict = {'one': '1', 'two': '2', 'three': '3'};
// Dictionary Array: Integer
let int_dict = {'one': 1, 'two': 2, 'three': 3};
// Dictionary Array: Float
let float_dict = {'one': 1.0, 'two': 2.0, 'three': 3.0};
// Dictionary Array: Boolean
let bool_dict = {'isStudent': true, 'hasJop': false,'isMarray': false};
