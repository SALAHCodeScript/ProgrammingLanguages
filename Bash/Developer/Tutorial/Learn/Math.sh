#!/usr/bin/python3
import math

x1 = abs(1) # القيمة المطلقة
x2 = abs(-1) # القيمة المطلقة
y1 = round(1.2) # رتبة مقدار e.g 1.1 > 1 | 1.5 > 2
y2 = round(1.6) # رتبة مقدار e.g 1.1 > 1 | 1.5 > 2
z1 = math.ceil(1.2) # رتبة مقدار e.g 1.1 > 2 | 1.5 > 2
z2 = math.ceil(1.6) # رتبة مقدار e.g 1.1 > 2 | 1.5 > 2
a1 = math.floor(1.2) # رتبة مقدار e.g 1.1 > 2 | 1.5 > 2
a2 = math.floor(1.6) # رتبة مقدار e.g 1.1 > 1 | 1.5 > 1
sqrt = math.sqrt(4) # Square Root
power1 = pow(2, 2) # Power
power2 = math.pow(2, 2) # Power
PI = math.pi # PI
cos = math.cos(30)
sin = math.sin(30)
tan = math.tan(30)
log = math.log(1)
